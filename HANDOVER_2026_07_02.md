# RadioAI Studio Pro — Session Handover (2026-07-02)

> THE BIGGEST SESSION YET. Read order for a cold start:
>   1. `PROJECT_BIBLE.md` (architecture, 3 invariants, workflow)
>   2. THIS file (today's state + open items)
>   3. `ROTATION_AI_AUDIT.md` + `VERIFICATION_NOTES.md` (evidence)

## Pre-flight (paste output)
```
git log --oneline -3        # top = b0f1314 perf(scheduler)...
git status --short          # clean except design_refs/ (untracked by convention)
py -m pytest tests/ -q --co # ~920+ tests
```
Branch `native-pyqt6`, HEAD `b0f1314`, **0 unpushed** (22 commits today,
cac4adc..b0f1314 all on origin).

## TL;DR — what shipped today (all operator-verified or sim-proven)

1. **Rotation AI overhaul** (`894de44`) — approve survives ticks; scheduler
   HONORS plan decisions (picks first, rest-exclusion, never-stall, 60s TTL
   cache); NULL-category slots use all-songs pool. E2E sim 13/13.
2. **13 UI bugs** from the 20-screen FEATURE_SPEC audit (`e6ad249` + more):
   IJ hotkeys, sweeper preview, songs filters+LastPlayed, playlist drag,
   campaign type persist, SOTG conflict, rotation-health tooltip, CP footer.
3. **NEXT-chip freeze fix** (`5abfd74`) — consumed-aware `_static_next_after`
   shared by dispatch + preview. Operator confirmed on air.
4. **Stitcher**: engine wired into Studio (never fired before! `19eaa06`),
   then FULL **break-tease sequenced model** (`0db6b6a`): spot pending →
   pin next-3 visible queue songs → song EOS → tease block STANDALONE →
   spots → pinned songs air in order (crossfade path honors pin too,
   `c7fe0ce`). Queue tray keeps songs when spot lands (same commit).
5. **Auto-Hook scanner** (`407b666`) — energy-based chorus detection
   (`core/hook_scanner.py`); **392/393 songs hooked** (live run, 395s);
   Stitcher-screen button + auto-hook on mass import.
6. **Category Auto-Grid** (`2098da1` core + `3bcc1f1` UI) — THE new
   autonomy feature: tag categories with air-time (Edit Categories "AIR
   TIME" presets/custom) → permanent AUTO clocks (template: sweeper before
   every song + jingle) → grid fills itself (boot + day-rollover +
   Rebuild-Now in hub). Manual cells + force_clocks sacred; overlap →
   mixed clock; AUTO badges on grid. Live-deployed + production-verified.
7. **Autonomous auto-apply** (`17441cb`) — hub toggle + configurable HH:MM
   (Settings `rotation_ai_auto_apply_enabled`/`_time`); OFF = pure manual.
   Hub now renders ALL sister groups (was capped at 2).
8. **Safety net trilogy** after THREE real DB corruptions (2026-07-01/02,
   all from force-killed mid-WAL writes):
   - `ensure_database_health()` in core/paths — launch quick_check + daily
     auto-backup (Backups/auto/, keep 7) + auto-restore quarantine (`6dd2e68`)
   - conftest **live-DB shield** — tests run on a throwaway copy (`30f0eba`)
   - **single-instance guard** QLocalServer (`80f5728`); installer
     CloseApplications=yes (graceful)
9. **Perf fix** (`b0f1314`, last commit) — rotation consult was 0.44s/pick
   on the 316-song category; peek_next(5) burned ~2.2s UI-thread per
   refresh → sluggish app + STARVED scheduler tick (frozen break countdown,
   missed 17:40 spot). Consult now dispatch-only (peek uses fast ladder via
   the `_suppress_queue_emit` flag). Measured 2.2s → 0.01s.
10. Misc: placeholder-artist exemption (`PLACEHOLDER_ARTISTS` — "Unknown
    Artist" 115 songs no longer veto each other); per-song log_play dedupe
    guard; dict(sqlite3.Row) repo-wide sweep; DB cleanup (799 dup broadcast
    rows removed, 5 phaseC-* categories deleted); docs committed
    (Bible/FeatureSpec/VerificationNotes/RotationAudit/CODE_SIGNING).

## Current LIVE state (operator's machine)
- **Installer latest = Jul 2 17:52** (`installer/RadioAI_Studio_Pro_Setup_v2.0.0.exe`)
  — operator was told to reinstall it (has the perf fix). VERIFY he did.
- DB healthy (`integrity_check: ok`). Backups: `Backups/auto/` (daily,
  self-managed) + several manual `radioai.db.*_bak*` artifacts from today.
- Grid: evening 18-23 all days = clock 3701 (MANUAL, sacred). **Demo tag
  active**: category 9 "Test 500" → 17:00-18:00 daily → clock 4031
  "AUTO · Test 500" (7 auto cells). Operator can remove via Edit
  Categories → Test 500 → × on the pill.
- Rotation plan 489 approved; auto-apply ON @ 17:00; sister groups 701
  (HotCurrents+Currents+Recurrents) + 702 (Gold+PowerGold).
- Staff testing in progress — expect bug reports.

## 🔴 OPEN ITEMS (priority order)

1. **Frozen-exe logging — PARTIALLY solved, verify next session.**
   The binary is PROVEN good: launching the dist exe writes the log
   perfectly incl. the boot sentinel (`frozen=True`). But the operator's
   17:34 session wrote NOTHING (no sentinel, no bootstrap_error.txt) —
   launch-context anomaly, unexplained. Next session: check
   `%LOCALAPPDATA%\RadioAI Studio Pro\Logs\radioai.log` for a fresh-session
   sentinel after his reinstall/relaunch. If STILL silent: inspect his
   shortcut target/elevation; the hardened logger (core/logger.py) will
   capture setup failures to `Logs/bootstrap_error.txt`.
2. **Break-tease on-air evidence** — pending a logged session. Grep the log
   for `[stitcher] tease armed` / `tease block ON AIR` / `playing promised`
   and match the three lists.
3. **Rapid-song bursts** — DB shows 3 songs starting within 21s
   (17:36:00/11/21). Likely operator clicks during testing, but if it
   recurs in logs, investigate EOS-error loops (file errors → instant EOS).
4. **Code-signing cert** — operator must PURCHASE (docs/CODE_SIGNING.md;
   Azure Trusted Signing ~$10/mo recommended). Then wire signtool into
   the build.
5. **Sweepers data** — all 4 active sweepers have unplayable file_path
   (RR_SW's source file vanished). Feature-side hardened; operator should
   attach real audio. This also makes
   `test_rotation_pickers::test_pick_sweeper_random` fail (data-dependent).
6. **Categories need songs** — operator plans 30+ per category (currently
   4-20). Auto-grid + rotation need the pool depth.
7. Pre-existing modal `.exec()` test hangs (sweepers/categories add-new
   paths) — run test files individually; known BASS teardown segfault
   flake also persists (shield makes it harmless to data now).
8. Stitcher trigger modes Every-N-songs / Top-of-hour save but have no
   consumer (only break_reference wired) — build if operator asks.
9. Edit Categories audit leftovers: Auto-Rotate / Min-Separation inputs
   still not persisted (pre-existing).

## Hard-won lessons (DO NOT REPEAT)
- **NEVER force-kill (taskkill /F) any process holding the live DB** —
  caused all three corruptions. Graceful: `CloseMainWindow()` via
  PowerShell, or TaskStop for harness tasks.
- Tests are now SAFE (shield) but engine tests still delete "today's
  plan" on their COPY — never point them at the live DB manually.
- The app is single-instance now: a second dev launch just raises the
  installed app's window. Close the installed app before `py main.py`.
- Rebuild recipe: `py -m PyInstaller RadioAIStudioPro.spec --clean
  --noconfirm` then `"$LOCALAPPDATA/Programs/Inno Setup 6/ISCC.exe"
  RadioAI_Setup.iss` → `installer/*.exe`.
- Heavy per-pick work must NEVER run in peek simulations — gate on
  `_suppress_queue_emit` (Invariant #3's flag pulls double duty).
- All 3 locked invariants (route_via_mixer=False, _has_pending_dispatch
  gate, _suppress_queue_emit) verified green after every change today.

## Where the new code lives
| Piece | Files |
|---|---|
| Auto-Grid | `core/auto_grid_builder.py`, db methods `*_daypart*`/`auto_grid_*`, hooks in `main.py` (boot) + `core/scheduler/engine.py` (rollover), UI in edit_categories_dialog / auto_schedule / scheduling_automation_hub |
| Hook scanner | `core/hook_scanner.py`, button in `ui/stitcher.py`, import-hook in mass_import_dialog |
| Break-tease | `ui/studio.py`: `_arm_stitcher_tease`, `_maybe_play_tease_first`, `_on_stitcher_tease_done`, `_pop_next_teased_song`, `_static_next_after` |
| Self-heal | `core/paths.py: ensure_database_health`, called from main.py boot |
| Auto-apply | `ui/main_window.py: _check_ai_rotation_auto_apply` (settings-driven), hub toggle |
| Consult gate | `core/scheduler/engine.py` step 3.5 (`_suppress_queue_emit` guard) |

**Jai Shree Ram. The radio runs itself now — keep it that way.**
