# RadioAI Studio Pro — Session Handover (2026-05-14)

> Read this end-to-end before touching any file. The previous session
> (2026-05-13) handed off at HEAD `006a9f4` with 3 uncommitted Stitcher
> pieces on disk and 495 tests collected. This session shipped **13
> commits**, lifted the test count to **568**, and built **7 new
> screens** including 2 designed directly in Figma via the MCP
> plugin API. Boot-time auto-play + live RJ mic talk-over both
> working end-to-end. No uncommitted work on disk.

---

## TL;DR (read this first)

- **Branch:** `native-pyqt6` (active dev, push here)
- **HEAD:** `dbf16cf` "feat(studio): MIC live toggle on Studio wordmark
  pill — ducks deck for RJ talk-over"
- **Tests collected:** **568 total** (`py -m pytest tests/ -q --co`).
  Was 495 at session start (+73 net after deleting 4 orphan tests +
  adding 77 new). Pre-existing modal-dialog hang in
  `test_playlists_screen.py` still permanently deselected; full suite
  minus that file = clean green when files run individually. BASS
  segfault flake still triggers when multiple BASS-touching test
  files run in the same process (e.g. `test_stitcher_screen.py` +
  `test_studio_stitcher_trigger.py` together) — each file passes in
  isolation. This is pybass3's DLL teardown, not the test logic.
- **App:** boots clean, **auto-plays on launch** when a clock is
  assigned to the current (weekday, hour) cell. Every screen the
  previous session shipped still works; this session added the full
  Stitcher screen, the Final Log Creator, the entire Settings
  hierarchy (Hub + 3 sub-pages), Play History per-song analytics,
  and the live MIC talk-over toggle.
- **Operator:** Kavish Sharma · station name flows from
  `Settings.station_display` (currently "Radio Rajasthan 90.8" —
  operator re-branded mid-session; was "FCP 90.8 MHz" / "KISS FM 91.5"
  earlier).
- **Hardware target:** 1366×768 desktop monitor.
- **Headline features shipped this session:**
  - **The Stitcher screen** (Figma 46:481) — pre-mix "Coming Up Next"
    hook montage + DB config + Studio pre-break trigger + true song-
    to-song crossfade overlap.
  - **Final Log Creator** (Figma 14:2 from a second Figma file
    `3pYLPe3k86I5hxJNubq9iR`) — broadcast-history viewer reading
    `broadcast_log`.
  - **Settings hierarchy** — Hub (designed via Figma MCP at 426:3) +
    General (68:2) + Soundcard (68:394) + Studio (69:2). Studio
    Settings are LIVE-applied to playback (master vol, fade-out
    threshold, crossfade duration, fade curve, missing-file alert,
    fallback action, NowPlayer overlays).
  - **Play History** (Figma 437:3, designed via MCP) — per-song
    analytics screen with 12-month bar chart + recent plays table.
  - **Boot auto-play** — software opens → first song from active
    clock plays automatically (no manual Open Studio + Play click).
  - **Songs Library upgrades** — drag-select + Ctrl/Shift multi-
    select + batch delete; detail-panel tabs (Song Details / Audio
    Cues / Play History) now interactive.
  - **Up Coming queue selection** — single-click any card to drive
    positional INSERT / REPLACE / DELETE / ADD against that row.
  - **Library SEARCH & FILTER wiring** — was decorative, now real
    (Search button, Reset, 3 filter checkboxes all live).
  - **MIC live toggle** — wordmark pill ducks the deck to 15% so
    the RJ can speak over a quiet background bed.
  - **Live station-branding refresh** — saving station name in
    General Settings instantly updates the header label on every
    other mounted screen (no app restart).
- **Uncommitted on disk:** **NONE.** Every change shipped. Tracked
  files are the cumulative carry-overs from the previous session
  (`HANDOVER_2026_05_13.md`, `blueprint.md`, `design_refs/*.png`)
  which stay untracked by operator convention.

---

## Pre-flight protocol (mandatory — paste raw output)

```
git branch --show-current     # native-pyqt6
git status                    # only the carry-over untracked files
git log --oneline -5          # dbf16cf top, then 1728dbe / abc3249 / b9f14c8 / 7d04f72
py -m pytest tests/ -q --co   # 568 tests collected
```

If anything mismatches → STOP and surface in plain Hindi.

Studio-focused sweep (faster feedback during Studio-area work):

```
py -m pytest tests/test_studio_*.py tests/test_sweeper*.py \
              tests/test_jingle*.py tests/test_stitcher*.py \
              tests/test_settings_*.py tests/test_play_history*.py \
              tests/test_songs_library*.py tests/test_final_log*.py -q
```

Note: this combo MAY hit the BASS-DLL segfault (see TL;DR). If it
does, run files individually — each one passes solo.

---

## Project basics

- **Name:** RadioAI Studio Pro v2.0
- **Stack:** PyQt6 (pure native widgets — NO HTML/WebView/Flask/JS),
  BASS audio via `pybass3`, SQLite WAL.
- **Project root:** `E:\RadioAI_v2` (Git Bash sees as `/e/RadioAI_v2`)
- **Modeled on:** Jazler SOHO (broadcast operator's mental model)
- **Run:** `py main.py` (or `py main.py --debug` for verbose logging)
- **Python:** 3.14, Qt 6.11
- **Note:** `pytest` is NOT on PATH — always use `py -m pytest`.
- **Canonical reference:** `blueprint.md` at project root (989 lines).
  §1 overview, §4 DB schema, §5 screen-by-screen features, §6 AI
  Engine, §7 audio engines (incl. Stitcher), §9 Figma reference
  table. **Read on cold start.**

---

## GitHub repository

- **URL:** https://github.com/kundansharma903-code/radioai-studio-pro
- **Branches:**
  - `native-pyqt6` — active development, push here
  - `main` — base / PR target. `blueprint.md` lives here.
  - `responsive-pilot-studio` — preserved failure record. **DO NOT DELETE.**
- **Push convention:** one focused commit per task on `native-pyqt6`.
- **Unpushed local commits:** previous session had 26 unpushed; this
  session added 13 → **39 unpushed local commits** on
  `native-pyqt6` as of HEAD `dbf16cf`. Operator has not requested a
  push at any point. **Confirm before pushing.**

---

## Database

- **Path:** `%LOCALAPPDATA%\RadioAI\radioai.db` →
  `C:\Users\hp\AppData\Local\RadioAI\radioai.db`
- **Schema:** `database/schema.sql` (idempotent `CREATE TABLE IF NOT EXISTS`)
- **Seeds:** `database/seeds.sql` (`INSERT OR IGNORE` only)
- **Manager class:** `core/database.py` (~2800 lines now) — thread-safe
  singleton.
- **Key tables (counts in dev DB at handover time):**
  - `songs` — 393 rows (was 396; 3 deleted in the session by the
    operator via the new multi-delete flow)
  - `clocks` — 35 active (clock_slots: 355); a "New test002" clock
    was assigned to weekday=3 (Thursday) hour 9 by the operator for
    auto-play testing.
  - `auto_schedule` — at least 1 cell now (was 0); the operator
    assigned a clock mid-session.
  - `campaigns` — 100 active
  - `spot_files` — 7
  - `jingles` — 19 master library + `jingle_pads` 3 with audio
  - `sweepers` — 4 active
  - `stitcher_config` — single row (id=1)
  - `settings` — **95 rows now (was 92)**. 3 new keys added on first
    General Settings load: `timezone`, `language`, `backup_interval`.
- **Schema columns added this session:** none — all helpers built on
  existing tables. **Defensive note**: the dev DB has
  `songs.entry_date` (older schema name); fresh installs from the
  current `schema.sql` get `songs.created_at`. The
  `get_song_play_history_summary` helper probes for both via
  `PRAGMA table_info` so it works on either shape.
- **DB helpers added this session (5):**
  - `get_stitcher_config()` / `update_stitcher_config(data)` /
    `STITCHER_EDITABLE_FIELDS` — single-row config for the Stitcher
    module (commit `9ffe1ab`). Initially had a `dict(row)` bug fixed
    in `dc561ca` — use `{k: row[k] for k in row.keys()}`, not
    `dict(row)`, for sqlite3.Row → dict conversion.
  - `get_broadcast_log_for_hour(year, month, day, hour)` →
    list[Row] — Final Log Creator (commit `553ff04`).
  - `get_broadcast_hour_counts_for_date(year, month, day)` → dict
    {0..23: count} — Final Log Creator.
  - `get_song_play_history_summary(song_id)` → dict — Play History
    (commit `abc3249`).
  - `get_song_monthly_plays(song_id, months=12)` → list — Play
    History.
  - `get_song_recent_plays(song_id, limit)` → list — Play History.
- **Inspect quick:**
  ```bash
  py -c "from core.database import Database; db=Database(); \
         print(db.get_dashboard_stats())"
  ```

---

## Figma reference

- **Primary file key:** `7oN9K61g94wKx3nu44KKDF`
- **File name:** "RadioAI — Control Panel PREMIUM"
- **Pages:** "Page 1" (default), "Studio Screens v3", "Scheduling
  Screens v2", "Ref Images Scheduling Tab", **"Settings Hub" (NEW)**,
  **"Play History" (NEW)**. The last two were created in-place via
  the Figma MCP plugin API by `use_figma` calls — see commits
  `dd2a3c3` and `abc3249`.
- **Secondary file key:** `3pYLPe3k86I5hxJNubq9iR` — operator-
  supplied design for **Final Log Creator (14:2)** + neighbouring
  screens (Soundcards 68:394, Studio Settings 69:2). General
  Settings 68:2 is in the PRIMARY file. Both files are visited
  routinely now.

### Cumulative screen → node ID table

| Screen | Node ID | File | Status |
|---|---|---|---|
| Control Panel | `5:2`   | primary | ✓ built |
| Songs Library | `12:2`  | primary | ✓ built (drag-select + tabs wired this session) |
| Spots & Commercials | `35:2` | primary | ✓ built |
| Scheduling Hub | `50:2` | primary | ✓ built |
| Main Auto Schedule | `161:2` | primary | ✓ built |
| Clock Editor | `165:2` | primary | ✓ built |
| AI Magic | `170:2` | primary | not built |
| Spots AI Monitor | `174:2` | primary | not built |
| Studio v3 | `312:2` | primary | ✓ built (MIC pill wired this session) |
| Sweepers Library | `46:2` | primary | ✓ built |
| Sweeper Editor Dialog | `108:2` | primary | ✓ built |
| Instant Jingles | `44:688` | primary | ✓ built |
| Jingles Library | `44:2` | primary | ✓ built |
| Add New Jingle Dialog | `106:2` | primary | ✓ built |
| **The Stitcher** | **`46:481`** | primary | **✓ built this session** |
| **General Settings** | **`68:2`** | primary | **✓ built this session** |
| **Soundcard Settings** | **`68:394`** | primary | **✓ built this session** |
| **Studio Settings** | **`69:2`** | primary | **✓ built this session** |
| **Settings Hub** | **`426:3`** | primary | **✓ designed via MCP + built** |
| **Play History** | **`437:3`** | primary | **✓ designed via MCP + built** |
| **Final Log Creator** | **`14:2`** | **secondary** | **✓ built this session** |
| Play Report Tab | `412:2` | primary | ✓ built |
| Play Report PDF Preview | `415:2` | primary | ✓ built |

### Frames NOT yet built (Figma exists, code doesn't)

- `170:2` AI Magic — engine view + decision log + category health
- `174:2` Spots AI Monitor — hourly ad usage donut, client rotation
- Stitcher screen Tabs 2 + 3 (Real Time Announcement, News Assembly)
  — placeholder "Coming soon" today
- `44:985` JingleGrid (alt card-grid view inside Jingles Library)

### Workflow when building a new screen

1. If the design is already in Figma → `get_design_context(fileKey,
   nodeId)` for metadata, `get_screenshot(fileKey, nodeId)` for the
   visual.
2. If the design is NOT yet in Figma but the operator wants one →
   use `use_figma(fileKey, code)` to build the frame programmatically
   via the Plugin API. Done twice this session for Settings Hub and
   Play History — both look pixel-clean and match the existing tokens.
3. Save screenshots to `design_refs/*.png` for reference (untracked).
4. Build PyQt6 — pure native widgets, no HTML/WebView.

---

## Architecture map (updates this session)

| Layer | File | Notes |
|---|---|---|
| Database | `core/database.py` (~2800 lines) | + Stitcher config helpers, + broadcast_log per-hour helpers, + per-song play history helpers (3 functions). Defensive against `songs.entry_date` vs `songs.created_at` schema variance. |
| Settings | `core/settings.py` | 95 keys now (was 92). New: `timezone`, `language`, `backup_interval`. Singleton + `.station_display` property unchanged. |
| Studio screen | `ui/studio.py` (~6500+ lines) | Most-touched file this session. + Stitcher pre-break trigger + shared `assemble_sequence`. + `_apply_studio_settings`, `_maybe_trigger_fade_out`, `_dispatch_crossfade_overlap`, `_handle_missing_file`, `_update_waveform_overlays` — five live consumers of Studio Settings keys. + `_fading_cid` field for true crossfade overlap (next song starts on a fresh channel while the outgoing channel fades). + `_NowPlayer.set_crossfade_overlay` + `set_mix_point_flash` + flash QTimer. + `_on_queue_row_selected` + `_target_queue_list` + positional INSERT/REPLACE/DELETE/ADD against the VISIBLE queue source. + Library `_apply_search_filter` + `_reset_search_filter` wiring. + MIC live toggle in `_Wordmark` + `_on_mic_toggled` / `_mic_engage` / `_mic_release` handlers. |
| Stitcher screen | `ui/stitcher.py` (NEW, ~1100 lines) | Figma 46:481. Tab 1 wired; Tabs 2-3 placeholder. |
| Final Log Creator | `ui/final_log.py` (~770 lines, REPLACED the orphan planner) | Figma 14:2 (secondary file). Reads broadcast_log per hour/date. DOWNLOAD .TXT works; PRINT LOG + background auto-save deferred. |
| Settings Hub | `ui/settings_hub.py` (NEW, ~370 lines) | Figma 426:3 (designed in-session via MCP). 3 option cards. |
| General Settings | `ui/settings_general.py` (~970 lines) | Figma 68:2. Body wrapped in QScrollArea so the save buttons stay reachable on 768px hardware. Emits `settings_saved` → MainWindow broadcasts `_refresh_station_branding` to every screen with a `findChild(QLabel, "hdr_station_lbl")`. |
| Soundcard Settings | `ui/settings_soundcard.py` (~720 lines) | Figma 68:394. BASS device enumeration via `BASS_GetDeviceInfo`. 7 real Windows devices surface on the dev box. v1 = DB save only; Test Tone + Test ALL + AudioEngine hot-reload are v1.1. |
| Studio Settings | `ui/settings_studio.py` (~970 lines) | Figma 69:2. 3-column layout in QScrollAreas. Each Save button writes only its column's keys. `settings_saved` → MainWindow → `studio._apply_studio_settings` → live runtime effect for master vol / fade-out / fallback / missing-file / NowPlayer overlays. |
| Play History | `ui/play_history.py` (~770 lines) | Figma 437:3. Hero card + 4 stat cards + 12-bar monthly chart (custom paint) + recent plays table (QTableWidget). |
| Main window | `ui/main_window.py` | + 6 new screen mounts. + `_apply_startup_auto_mode` (boot auto-play). + `_on_stack_currentChanged` (dynamic stack resize so smaller screens don't leave blank scroll space). + `_refresh_station_branding` (live station name propagation). + `_on_studio_settings_saved` (Studio Settings live-apply broadcaster). + Outer scrollbar restyled (transparent track, cyan hover). + Various new route cases (settings / settings_general / settings_soundcard / settings_studio / play_history / songs from breadcrumbs). |
| Confirm Delete Dialog | `ui/dialogs/confirm_delete_dialog.py` | + multi-song mode (`song_data_list` kwarg). Signal changed to `delete_confirmed = pyqtSignal(list)` — ALWAYS emits a list now, even for single-song deletions. |
| Songs Library | `ui/songs_library.py` | + drag-select infrastructure on `_SongRow` (`clicked(int, modifiers)`, `dragged(QPoint)`, `released()` signals). + `_selected_ids: set` + `_drag_origin_idx` state. + Ctrl/Shift modifier-aware `_select_song`. + `selected_song_ids()` public API for downstream batch ops. + Detail-panel tabs (Song Details / Audio Cues / Play History) now interactive — clicking Audio Cues opens the cue editor dialog, Play History emits `report_clicked("play_history")`. |

---

## Commits this session (newest → oldest)

| Commit | Theme |
|---|---|
| `dbf16cf` | **feat(studio): MIC live toggle on Studio wordmark pill — ducks deck for RJ talk-over** |
| `1728dbe` | **feat(studio): boot auto-play + scrollbar polish + dynamic stack resize + Up Coming row selection + library search wiring** |
| `abc3249` | **feat(songs): Play History screen (Figma 437:3) + Songs Library tab wiring + chrome polish** |
| `b9f14c8` | **feat(songs): drag-select + Ctrl/Shift multi-select + batch delete** |
| `7d04f72` | **feat(studio): Studio Settings (Figma 69:2) + live-applied playback wiring** |
| `dd2a3c3` | **feat(ui): Settings Hub (Figma 426:3) + Soundcard Settings (Figma 68:394)** |
| `bbec8d2` | **feat(ui): General Settings screen (Figma 68:2) + live station branding refresh** |
| `553ff04` | **feat(ui): Final Log Creator — broadcast history viewer (Figma 14:2)** |
| `dc561ca` | **fix(db): get_stitcher_config row→dict uses safe key-iteration pattern** |
| `c7ce51e` | **feat(studio): pre-break Stitcher trigger + shared assemble_sequence helper** |
| `9ffe1ab` | **feat(ui): The Stitcher screen (Figma 46:481) + DB config layer** |
| `f75d292` | **fix(ui): ControlPanel stats — Instant Jingles uses live jingle_pads, Stitcher uses live stitcher_active** |

(13 commits total. Two consecutive Songs Library commits got folded
into a single message-set; see `abc3249` for the combined work.)

---

## Operator-facing features — current state

### ✅ Working (operator confirmed end-to-end this session)

| Feature | Status |
|---|---|
| **Boot auto-play** | App opens → MainWindow nudges Studio's `_on_play_clicked` 150ms after the GUI paints → scheduler starts + first song from active clock dispatches. Skips cleanly when no clock is assigned to the current hour. Gated on `Settings().start_in_auto_mode` (default ON). Operator confirmed working with their newly-assigned clock "New test002". |
| **MIC live toggle** | Wordmark pill on Studio (top-right of MASTER strip). Click → green "● MIC ON" + deck volume fades to 15% over 600ms. Click again → back to "MIC OFF" + restore to pre-mic volume. Pure software ducking — RJ's physical mic assumed to be on a hardware mixer in parallel with RadioAI. |
| **True song-to-song crossfade** | Studio Settings Fade Out Start / Crossfade Duration drive a real overlap: at the trigger point the next song starts on a fresh channel WHILE the outgoing channel keeps fading out. `_fading_cid` tracks the outgoing channel for silent cleanup on its EOS. Per-song `mix_point_ms` overrides the global `fade_out_start` setting when set. |
| **The Stitcher** | Tab 1 fully wired (config save + sample playlist + ▶ Preview Full). Studio fires the stitcher block on `break_approaching` with deck duck → 20% → restore on done. Tabs 2+3 placeholder. 0/393 songs have hook_in_ms/hook_out_ms set in the dev DB — the engine will skip-fire until the operator authors hooks. |
| **Final Log Creator** | Date picker + FETCH REPORT + 24 hour-slot list with counts + per-hour table + stats strip + DOWNLOAD .TXT (QFileDialog with `%LOCALAPPDATA%\RadioAI\logs\<Year>\<Month>\<DD>\<HH>-<HH+1>.log` default). PRINT LOG = v1.1 placeholder. |
| **General Settings** | 6 station-identity fields + 4 file-path Browse pickers + 7 startup toggles + 4 date/time dropdowns + backup card + 2 Save buttons. Save broadcasts `settings_saved` → MainWindow `_refresh_station_branding` walks every mounted screen + updates `findChild(QLabel, "hdr_station_lbl")`. paintEvent-based headers (control_panel, studio) get `.update()`. Station name change visible everywhere instantly — no restart. |
| **Settings Hub** | Operator-facing landing page for the 3 settings sub-sections. Every "Settings" entry-point in the app routes through this hub first. Designed directly in Figma via MCP this session (page "Settings Hub", node 426:3). |
| **Soundcard Settings** | 5 channel cards (4 outputs + 1 input) with real BASS device enumeration. Each card: device dropdown, volume slider 0-100, latency placeholder, channel mode, Test Tone (v1.1 toast), connection status. Save persists to `audio_output_X` / `audio_vol_outputX` / `audio_channel_outputX` keys. AudioEngine hot-reload is v1.1 (restart required for driver change). |
| **Studio Settings** | 3 columns × 3 cards each. 5 keys are LIVE-applied via `_apply_studio_settings`: master_volume, crossfade_duration, fade_out_start, fade_curve_type (read but linear only today), missing_file_alert, fallback_action. Plus show_crossfade_preview + flash_mix_point drive `_NowPlayer` overlay rendering. Plus mix_point_ms override read on every position tick. Save → broadcast → live effect with no restart. |
| **Play History** | Per-song analytics. Hero card with TOTAL PLAYS giant number + 4 stat cards (this month/week/avg/last) + 12-bar custom-paint monthly chart (current month cyan, peak month purple) + 5-row recent plays table joined with clock names. Reaches via the Songs Library "Play History" detail-panel tab OR the report-action button. |
| **Songs Library drag-select** | Plain click replaces; Ctrl+click toggles; Shift+click range; LMB-held drag extends selection by mapping global mouse pos back to row geometry. `selected_song_ids()` is the public read-side for future batch ops. |
| **Songs Library multi-delete** | ConfirmDeleteDialog now accepts `song_data_list` and emits `delete_confirmed = pyqtSignal(list)` — always a list, single-song mode included. Batch deletes iterate `db.delete_song` per id, summary toast on partial failure. |
| **Songs Library detail tabs** | Song Details / Audio Cues / Play History tabs now interactive. Audio Cues opens the existing AudioCueEditorDialog (same target as the ✎ Edit Cues button). Play History routes to the new screen with the currently-selected song. Underline animates between tabs. |
| **Up Coming queue row selection** | Single-click any populated card → cyan border highlight + emits `row_selected(idx, song)`. Selection preserved across the 1Hz refresh by id+type match. Studio captures the selection + reroutes library actions: ADD = insert AFTER, INSERT = before, REPLACE = at position, DELETE = drop that row. All actions mutate the VISIBLE queue (auto-routes between `_upcoming_preview` scheduler-driven path and `_queue_songs` legacy path via `_target_queue_list`). |
| **Library SEARCH & FILTER** | Was decorative; now real. Search button + Enter key apply substring filter on `_all_rows`. Reset clears + restores. SuperSearch / Show Only NEW / Sort by Surname checkboxes drive re-filter on toggle. "N RESULTS" header repaints. Section moved from x=14 to x=102 so the heading no longer paints into the DELETE action button. |
| **Outer scrollbar polish** | MainWindow QScrollArea restyled: transparent track, 10px outer width with 3px handle inset (reads as a thin 4px bar), 60px minimum handle, subtle gray idle, cyan accent on hover, brighter cyan on press. No add-line/sub-line stubs. |
| **Dynamic stack resize** | `_on_stack_currentChanged` shrinks/grows the QStackedWidget to the active screen's footprint. 1440×900 screens no longer leave 480px of blank scrollable space past their right edge. Studio + Final Log still get their 1920×1080 reach. Safety floor 800×600. |

### 🟡 Partially working / known limitations

- **0/393 songs have hook_in_ms/hook_out_ms** — Stitcher trigger
  fires but assembler returns `[]`. Operator workflow: Songs Library
  → song row → Audio Cues tab → set Hook In/Out. Bulk-hook-set helper
  is the operator-blocker carry-over.
- **Stitcher Tabs 2 + 3** — Real Time Announcement / News Assembly
  placeholder.
- **Soundcard Test Tone / Monitor / Test ALL** — v1.1 toast.
- **AudioEngine hot-reload from Soundcard Settings** — needs restart
  today.
- **Studio Settings non-linear fade curves** — Logarithmic / Exp /
  S-Curve are logged but the underlying `engine.fade_volume_to` is
  linear. Curve interpolation is an AudioEngine extension.
- **Studio Settings AutoCue threshold / scan mode** — Settings save
  but no auto-cue-point-detection engine exists yet.
- **Studio Settings VU decay / peak hold / clip indicator** —
  Settings save but no live VU widget consumes them.
- **Studio Settings Cue Split Mode** — single-output BASS path only
  in production; setting saves but routing is single-output.
- **General Settings Backup Now / Restore** — v1.1 toast.
- **MIC live = software ducking only.** Real system-mic input
  capture via BASS_RecordStart + mixing into RadioAI's on-air
  output entirely in software is v1.1. Today's assumption: RJ's
  physical mic is on a hardware mixer alongside RadioAI.
- **Final Log Creator PRINT LOG + background hourly auto-save** —
  v1.1.
- **Stitcher Set-Hook deep-link** — toasts pointing to Songs Library.
- **Stitcher AI Insights real metrics** — "Times Used Today" / "Avg
  Listener Retention" still placeholders. Songs Without Hooks is live.
- **AUTO pill at x=1464** — may be off-screen on 1366×768.
  Workaround: launch maximized.

### 🔴 Not yet built

- **AI Magic screen** (Figma 170:2)
- **Spots AI Monitor** (Figma 174:2)
- **Stitcher Tabs 2 + 3** (Real Time Announcement, News Assembly)
- **Frame 12 (294:2) — Final Log Creator Day Picker** (different
  from the 14:2 history viewer that landed this session)
- **Phase 2 Part 2** responsive layout for non-Studio screens
- **AI Daily Scheduler** integration with Studio
- **RDS / Icecast streaming** output — wire SIGNAL pill
- **Stitcher broadcast_log instrumentation** — log every fire
- **Bulk hook-set helper** — operator's actual blocker for the
  Stitcher's real-use value
- **AI hook detection** — v1.3 roadmap
- **Campaign picker dialog** — opened by Jingle Editor's LINKED
  SPOTS + button
- **Soundcard Settings: real Test Tone playback + AudioEngine
  hot-reload from saved settings**
- **Studio Settings: AutoCue engine, live VU widget,
  multi-output cue-split routing, non-linear fade curve
  interpolation**
- **MIC live: real BASS_RecordStart-based mic mixing** (v1.1)
- **Final Log Creator: PRINT LOG, background hourly .txt auto-save**
- **library_chrome.py extraction** — 6+ libraries share chrome
  widgets now; refactor threshold long-since crossed but it works
- **Push to GitHub remote** — 39 unpushed local commits on
  `native-pyqt6`. Operator hasn't asked for push at any point.

---

## NIGHT_LOG carry-overs (forensic record)

Read `NIGHT_LOG.md` end-to-end before starting major new work. Key
past incidents (cumulative across sessions):

1. **2026-05-04 — DELETE-with-LIKE wipe** — `LIKE '__%'` matched all
   rows ≥2 chars long → nuked 15 campaigns + 791 schedule rows.
   "Destructive operation protocol" in CLAUDE.md was the response.
2. **"Saved clock vanished" display bug**.
3. **Pre-existing modal-dialog hung test** — permanently deselected.
4. **Studio v3 9-step rebuild** discipline checkpoints.
5. **Phase 2 responsive pilot canceled** — `responsive-pilot-studio`
   preserved.
6. **NEXT chip cursor-eat bug** — `_compute_next_song` destructive vs
   `_peek_next_for_display` non-destructive.
7. **BG_DARK NameError crash** — undefined token in stylesheet f-string.
8. **Edge "File not found" race** — QPdfWriter handle flush.
9. **QPainterPath missing import crash** — paintEvent ran code that
   tests didn't exercise; live app crashed exit-127 silently.
   **Lesson:** run main.py for any paint-heavy change.
10. **ControlPanel "Instant Jingles" wrong stat key** — `jingles_total`
    vs `jingle_pads` semantics.
11. **(2026-05-14, this session) `dict(sqlite3.Row)` raises**
    `ValueError: dictionary update sequence element #0 has length 14;
    2 is required` on Python 3.14 + pybass3's Row factory. Symptom:
    every test in `test_studio_stitcher_trigger.py` crashed in its
    cfg_snapshot fixture. Fix: replace `dict(row)` with
    `{k: row[k] for k in row.keys()}`. Sibling pattern already at
    line 2168 — use that for ALL row→dict conversions. Fixed in
    commit `dc561ca`.
12. **(2026-05-14) BASS DLL multi-load segfault flake** — running
    `test_studio_stitcher_trigger.py + test_stitcher_screen.py`
    together in one pytest process produces a "Windows fatal
    exception: access violation" mid-suite. Each file passes in
    isolation. Pre-existing pybass3 lifecycle issue, NOT this
    session's bug. Workaround: run files individually when verifying.
13. **(2026-05-14) Songs Library Play History breadcrumb broke "Songs
    Library" link** — `_on_hub_screen_requested` had no `"songs"`
    case; user clicked breadcrumb and nothing happened. Fix landed
    inside `abc3249`. **Lesson:** every new screen's breadcrumb
    crumbs must have a matching case in either `_on_breadcrumb`
    (control_panel only) or `_on_hub_screen_requested` (everything
    else). New screens routing to `_on_hub_screen_requested` is the
    convention now.
14. **(2026-05-14) Studio queue mutations invisible to operator** —
    INSERT/REPLACE/DELETE on the Up Coming queue mutated
    `_queue_songs` while the panel rendered `_upcoming_preview` (the
    scheduler-driven path). Operator saw nothing happen. Fix:
    `_target_queue_list()` helper auto-routes to whichever list is
    driving the visible panel. **Lesson:** when a screen has dual
    data sources for one widget, mutations need to target the
    VISIBLE source, not the legacy one.
15. **(2026-05-14) `_LibrariesPanel.SEARCH & FILTER` section painted
    over DELETE button** — search input was at x=14, same column as
    the action stack (x=14, width 76). Visual bleed-through. Fix:
    moved entire section + heading to x=102 (aligned with songs
    table column). **Lesson:** in two-column panels, always confirm
    no widget straddles the column boundary.

---

## Established conventions (discipline rules — follow without being told)

### Communication style

- **Hindi-English mix** is fine and preferred for operator-facing
  summaries.
- **Plain Hindi** required for verification questions to operator.
- **Senior-dev pushback ON** — surface contradictions between prompt
  and source code before editing.
- **Investigation-first** — read 3-4 files, post 4-line summary,
  wait for confirmation BEFORE editing. Applied 100% of the time
  this session. Operator never complained.
- **Trust-but-verify** — source code wins over docs/prompts.
- **Brief responses** — narrate intent in 1-2 sentences before tool
  calls; long planning prose annoys operator.
- **End-of-turn summary:** one or two sentences. What changed +
  what's next.

### Coding style

- **Cache QGradient/QColor/QFont in `__init__`** (performance invariant)
- **Partial repaints via `self.update(QRect)`** not bare `self.update()`
- **NO db calls in paintEvent**, NO `self.update()` inside paintEvent
- **Drop shadows via `QGraphicsDropShadowEffect`** (one per widget)
- **Engine signals throttled** — position ≤4Hz, level meters ≤30Hz
- **Try/except every scheduler `_on_tick` path** — broadcast safety
- **`hasattr(scheduler, "signal_name")` defensive guards** for new
  scheduler signals — backward compat with mocks
- **Settings is a singleton** — `Settings().station_display` from any module
- **Inter font family is "Inter Variable"** (not "Inter")
- **PDF generators must `del writer` + `gc.collect()`** before
  returning — Windows / Edge race
- **`os.startfile()` on Windows** for opening files
- **Self-contained screen files duplicate chrome widgets** — 8
  libraries / settings screens now share `_HeaderLogo` /
  `_HeaderOpenStudio` / `_BreadcrumbLink` / `_BreadcrumbPill` /
  `_StatusPill`. Extraction to `ui/widgets/library_chrome.py` is the
  perpetually-deferred refactor. **DO this before the 9th library lands.**
- **(NEW) `dict(sqlite3.Row)` is BANNED.** Use
  `{k: row[k] for k in row.keys()}`. Python 3.14 + pybass3 row
  factory chokes on the former. See `dc561ca`.
- **(NEW) When two data sources drive one panel** (e.g.,
  `_upcoming_preview` + `_queue_songs`), mutations need a
  `_target_queue_list()`-style helper that picks the VISIBLE one.
- **(NEW) Every new screen that has a `hdr_station_lbl` QLabel
  must be added to MainWindow's `_refresh_station_branding`
  `qlabel_screens` tuple** so live station name changes propagate.
- **(NEW) Every new screen's breadcrumb crumbs must be routed in
  `_on_hub_screen_requested`** (or `_on_breadcrumb` for the literal
  control_panel crumb). New keys land here: "control_panel",
  "songs", "libraries" / "scheduling_hub" / "settings" /
  "settings_general" / "settings_soundcard" / "settings_studio" /
  "final_log_creator" / "play_history" / "studio_open".

### Test discipline

- **Live-DB fixtures use unique prefix** + `try/finally` cleanup
- **Mock pattern:** `_FakeEngine` / `_FakeScheduler` / `_FakeIJE` /
  `_RecordingSignal` / `_FakeStitcher` etc.
- **Don't modify existing test files** — add new ones
- **Run focused tests after each piece** before full suite
- **Full suite must stay green** — zero regressions tolerance
- **Wall-clock-boundary flake** in
  `test_live_at_tick_refreshes_via_existing_1hz_timer` — pre-existing.
- **Single-row config tables need snapshot + restore in fixtures** —
  `stitcher_config` is the canonical example.
- **Test fakes that pass file_path strings need real on-disk paths**.
- **(NEW) For Songs Library widget tests that need real layout
  geometry**, fixture must `s.show()` + `qapp.processEvents()` +
  `s._table_layout.activate()` before yielding. Drag-select tests
  need `.geometry()` to return non-zero. See
  `tests/test_songs_library_multi_select.py`.
- **(NEW) Studio integration tests need a `_FakeScheduler` that
  carries ALL signals Studio expects to `.connect` to** — currently
  10+: queue_song_play, scheduler_state_changed, break_approaching,
  spot_due, song_auto_advance, next_break_in, started, stopped,
  sweeper_dispatch, tick, active_clock_changed, queue_updated. Skip
  one and Studio.__init__ AttributeError-crashes the fixture.

### Commit discipline

- **One focused commit per task** — heredoc commit message format
- **Commit message body:** problem + root cause + fix + test count
  delta. Include "Co-Authored-By: Claude Opus 4.7 (1M context)"
- **No `--no-verify` / `--amend`** — always new commits
- **Push to `native-pyqt6` only**
- **Don't `git add -A`** — explicitly add files
- **Operator-confirmed-working before commit** — every commit this
  session followed an explicit "perfect / great / commit this" from
  the operator. Don't commit on test-only evidence.

### Destructive ops protocol (CRITICAL)

- **Never use `LIKE` patterns** for cleanup queries
- **Always run `SELECT COUNT(*)` first** with the same WHERE clause;
  get explicit user confirmation
- **Wrap in `BEGIN TRANSACTION`** for multi-row cleanup
- **Use `db.delete_*()` methods** for tables without `ON DELETE CASCADE`

### Locked code paths (DO NOT MODIFY without explicit approval)

- `core/audio/*` — Phase A LOCKED (`get_levels` is the carve-out)
- `core/instant_jingle_engine.py` — `fade_stop_pad` is the only
  recent add
- `ui/studio_legacy.py` — rollback insurance
- `ui/instant_jingles.py` (standalone screen) — legacy ctor branch
  preserved
- Existing test files

---

## Quick start for next session

1. Run pre-flight (paste output).
2. Read this file end-to-end.
3. Skim `CLAUDE.md` (project guide, Figma frames, destructive
   op protocol — the Figma table is now stale, see this handover's
   table for the current state).
4. **Skim `blueprint.md`** especially §7 Audio Engine, §5.7
   Scheduling, §6 AI Engine.
5. Skim the most recent ~5 entries of `NIGHT_LOG.md` (top entries
   from this session were folded into the incident list above too).
6. Check git log to confirm HEAD matches `dbf16cf` and tests = 568.
7. **Wait for operator's task instruction** — do NOT proactively pick
   up pending items.

When the operator gives a task:
- **If it touches Studio** → read the relevant section of
  `ui/studio.py` (~6500+ lines; navigate by class name + grep). New
  surfaces this session: `_apply_studio_settings` block,
  `_dispatch_crossfade_overlap`, `_handle_missing_file`,
  `_update_waveform_overlays`, `_on_queue_row_selected`,
  `_target_queue_list`, `_on_mic_toggled` and friends.
- **If it touches Settings** → 3 screens + 1 hub. General is the
  reference implementation; Soundcard is reduce-to-hardware-form;
  Studio Settings is the one that LIVE-applies to playback.
- **If it touches the Stitcher** → `core/stitcher_engine.py` +
  `ui/stitcher.py` + Studio's `_dispatch_crossfade_overlap` +
  `_maybe_trigger_fade_out`. `assemble_sequence` is shared.
- **If it touches songs / play history** → `ui/songs_library.py`
  + `ui/play_history.py`; broadcast_log helpers in
  `core/database.py`.
- **If it's a new screen** → fetch Figma context first; if no
  Figma design exists, ask the operator whether to design via the
  MCP plugin API (`use_figma`) before building.
- **Always investigation-first, 4-line summary, wait for
  confirmation, then implement.**

### Likely next-session topics (operator-driven, but plan ahead)

- **Push 39 local commits to GitHub** (operator approval needed)
- **Bulk hook-set helper** — operator's actual Stitcher-blocker
- **AI Magic screen** (Figma 170:2)
- **Spots AI Monitor** (Figma 174:2)
- **Stitcher Tabs 2 + 3** — Real Time Announcement, News Assembly
- **Stitcher broadcast_log instrumentation** — Times Used Today stat
- **Rotation Health / Last Played / Top Songs** — the other 3 Songs
  Library report tiles next to Play History (similar structure
  expected). Songs Library "📊 Play History" tile is `report_clicked`
  key `play_history`; siblings would emit `rotation_health` /
  `last_played` / `top_songs`. Need new screens for each, plus DB
  helpers similar to `get_song_play_history_summary`.
- **Soundcard Test Tone real implementation** — temp WAV gen + BASS
  one-shot play on the selected device
- **AudioEngine hot-reload from Soundcard Settings** — driver re-init
  without restart
- **Real BASS_RecordStart-based mic mixing** — full software mic-in
  path for stations without hardware mixers
- **Final Log Creator background hourly auto-save** — top-of-hour
  cron-style writer producing `Logs/<Year>/<Month>/<DD>/<HH>-<HH+1>.log`
- **library_chrome.py extraction** — long overdue; 8+ libraries share
- **Phase 2 responsive layout retry** —
  `responsive-pilot-studio` preserved
- **AI hook detection** — v1.3 roadmap
- **Campaign picker dialog** — Jingle Editor's LINKED SPOTS + button

---

## Operator profile

- **Name:** Kavish Sharma
- **Station name flows from `Settings.station_display`** — currently
  "Radio Rajasthan 90.8" (operator re-branded mid-session).
- **Location:** Sikar / Jaipur, Rajasthan, India (replacing Jazler SOHO)
- **Hardware:** 1366×768 desktop monitor.
- **Communication style:** Hindi-English code-switch is normal.
  Senior-dev pushback expected. Patient with disciplined work but
  pushes back on scope creep, over-engineering, and silent
  papering-over.
- **Workflow expectation:** investigation-first, 4-line summary,
  wait for confirmation BEFORE editing. Don't claim audio/visual
  outcomes you haven't verified.
- **Verbal cues:**
  - "go" / "ok" / "Process" / "confirm" → proceed
  - "great" / "good" / "perfect" → confirmed, move on
  - "commit this" / "commit all" → commit now
  - "run main.py" / "launch main.py" → launch app for visual smoke
  - "its not working" → root-cause investigation, no quick patches
  - "do one thing" → small focused task incoming
  - "kar do" → just do it (skip lengthy planning)
  - "isko rehne do" → defer / skip this item

### Observations on operator's working pattern (this session)

- **Designs from Figma first, ALWAYS.** Operator always linked to a
  Figma node before building. When a screen wasn't yet designed
  (Settings Hub, Play History), they asked the agent to design it
  in Figma via MCP first, then approve, then build. Don't skip the
  design step.
- **Honest scope cuts work.** Multiple times this session the agent
  pushed back on which fields are "real wire" vs DB-save-only and
  the operator confirmed and shipped. They appreciate transparency.
- **Smoke-test cycle:** every feature gets verified by the operator
  visually via `py main.py` before commit. Don't commit on test
  evidence alone.
- **Iterates rapidly on visual issues.** Found the SEARCH & FILTER
  overlap, the bottom horizontal scroll-into-blank-space, the queue
  click-not-selecting all in one sitting — surface visual bugs
  immediately when noticed.
- **Doesn't push to GitHub.** 39 unpushed commits and counting. Don't
  proactively suggest a push.

---

## Last words

This session was front-to-back screen builds + live-wiring. The
biggest leap is the Studio Settings → Studio playback live path —
five separate `_cfg_*` consumers (master vol, fade-out, crossfade
overlap, missing-file fallback, NowPlayer overlays) all driven from
one settings-save broadcast. That pattern is the blueprint for the
remaining live-wiring work: Soundcard Settings → AudioEngine hot-
reload would follow the same shape.

The MIC live toggle is the smallest commit but the highest-impact
operator-facing feature — one click puts an RJ on air. The real
software-side mic mixing (BASS_RecordStart) is the obvious v1.1
follow-up but the hardware-mixer assumption today is correct for
how the operator's actual broadcast chain works.

The Settings Hub + Play History designs were both authored in
Figma via the MCP plugin API in-session — that's the new workflow
for any operator-requested screen that doesn't yet exist as a
design. Cleaner than guessing at intent; faster than waiting for a
designer.

**The single most important thing for the next session:** before
touching anything new, run pre-flight + read this file + read the
last 5 entries of NIGHT_LOG.md. Don't pick up pending items
proactively — wait for the operator.

The 39 unpushed commits to GitHub remain unpushed. Don't push
without an explicit operator request.

Good luck.

— Previous session, signing off at HEAD `dbf16cf` with 568 tests
  collected, app boots clean, operator confirmed every feature
  working end-to-end on 2026-05-14.
