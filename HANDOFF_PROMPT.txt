═══════════════════════════════════════════════════════════════════════
RadioAI Studio Pro v2.0 — SESSION HANDOFF (Phase F kickoff)
═══════════════════════════════════════════════════════════════════════

Hi Claude. Resuming work on a previous session that hit token limit.
Project root: E:\RadioAI_v2\

STEP 1 — Read these two files in order, before anything else:

  1. E:\RadioAI_v2\PROJECT_CONTEXT.md       (macro — what is this project)
  2. E:\RadioAI_v2\CURRENT_TASK_STATE.md    (micro — exactly where we are)

Don't summarize them back to me. Just read them so you have the full
mental model.

STEP 2 — Once read, here's the immediate situation:

  • Active task: Phase F kickoff — Scheduling UI section (8 screens, ~7-8 days)
  • Decided build order: F2 Clock Editor (Figma 165:2) FIRST, then
    F1 Auto Schedule (161:2), then F3 Hub (50:2), then F4-F8.
  • Pre-planning report was DELIVERED (it's documented in
    CURRENT_TASK_STATE.md §"Pre-planning report"), but Q1-Q8 questions
    are NOT YET ANSWERED. I (the user) need to answer them before you
    can start F2.1 build.
  • Studio + scheduler are PRODUCTION-READY ("lagao chod do" capable).
  • Test count: 74/74 passing under pytest.
  • Phase D close-out commit + marker pushed to native-pyqt6.

STEP 3 — Your first action when I confirm I'm ready:

  Re-pose the 8 pending questions from CURRENT_TASK_STATE.md
  §"Pending decisions (Q1-Q8)". Wait for my answers + explicit "GO F2.1"
  before building anything.

  Don't auto-start coding. Don't auto-fetch Figma again — the design
  references are already inventoried in CURRENT_TASK_STATE.md. Just
  re-pose the questions concisely so I can answer them.

STEP 4 — While I'm answering, you can optionally:

  • Read ui/studio.py to refresh how the deck integration looks
    (Phase D's most recent screen, similar layout complexity to F2)
  • Read ui/songs_library.py for the row-table pattern (F2's slot
    list will use the same paint pattern)
  • Read core/database.py for clock-related queries (search "clocks"
    to find existing read paths)

═══════════════════════════════════════════════════════════════════════
HARD CONSTRAINTS — DO NOT VIOLATE
═══════════════════════════════════════════════════════════════════════

A) E:\RadioAI_v2\ IS a git repo, connected to
   github.com/kundansharma903-code/radioai-studio-pro on branch
   native-pyqt6. Push proactively after every phase day. See COMMIT
   POLICY below.

B) Destructive ops protocol (per CLAUDE.md): never DELETE/UPDATE with
   LIKE patterns on shared tables. Always SELECT-COUNT first, show me,
   wait for explicit confirmation. We had a real incident (DELETE-
   with-LIKE wiped 15 campaigns).

C) Performance invariants for custom-paint widgets are documented at
   the top of ui/dialogs/spot_programming_dialog.py,
   ui/dialogs/audio_cue_editor_dialog.py, and ui/studio.py. F2's
   slot list (clock_slots table view) MUST honor them — event.rect()
   clipping, bounded mouseMoveEvent, no nested QScrollArea, no DB
   calls in paintEvent.

D) Phase B audio surfaces (Songs Library row preview / Audio Cue
   Editor PREVIEW / Spots Now Airing / Instant Jingles pads) MUST
   keep working through Phase F. They share the AudioEngine
   singleton — F2 is data-only and shouldn't touch the engine.

E) Phase D's Studio + scheduler MUST keep working. F2 doesn't change
   scheduler behavior; it just edits the data the scheduler doesn't
   yet read (clock_slots — Phase E will wire those into the
   scheduler).

F) `core/scheduler.py` legacy file is SHADOWED DEAD CODE since Phase
   D3. Recommend deleting in a prep commit before F2.1 build. ASK
   before deleting.

G) `core/audio_engine.py` legacy single-deck class is unused but the
   module owns bass_init/bass_free. DON'T TOUCH unless explicitly
   asked.

H) Manual deck plays from Studio do NOT write to broadcast_log
   (audition only). Only scheduler-triggered spots write. This is
   the Q5/D4 contract — don't change it without explicit approval.

I) Database singleton — never instantiate Database() multiple times.
   Always Database() returns the same singleton. Connections are
   thread-local; the scheduler thread gets its own.

═══════════════════════════════════════════════════════════════════════
GITHUB CONNECTION + COMMIT POLICY
═══════════════════════════════════════════════════════════════════════

  • Active branch:   native-pyqt6 (canonical — DO NOT touch main)
  • Latest commit:   docs: Phase D complete — Studio + scheduler +
                     continuous play
  • Repo URL:        https://github.com/kundansharma903-code/radioai-studio-pro

After EVERY phase day (F2.1, F2.2, F2.3, F2.4, F1.1, ...) commit + push:

  1. git status (verify what's staged)
  2. git add <specific files>     # not always `git add .` — see below
  3. git commit -m "feat: Phase F<X>.<Y> — <short summary>"
  4. git push origin native-pyqt6

`git add .` is NOW SAFE since `.claude/worktrees/` was added to
.gitignore in commit `be9e15b`. Use it freely; it won't include the
worktree storage.

ALSO add `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`
trailer to every commit message.

DO NOT:
  • git init                  (already done)
  • git remote add            (already done)
  • Touch the `main` branch
  • Force push without explicit approval
  • git reset --hard without explicit approval
  • Skip hooks (--no-verify) unless I explicitly ask

DO use:
  • git status               (frequently — before any operation)
  • git log --oneline -5     (verify history)
  • git diff                 (before committing — show me what's staged)

═══════════════════════════════════════════════════════════════════════
COMMIT MESSAGE FORMAT
═══════════════════════════════════════════════════════════════════════

  feat: Phase F2.<day> — <short summary>

  <detailed body>:
    - What was built
    - Files changed
    - Test count delta
    - Notable decisions
    - Known issues / next steps

  Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>

Examples that have been used in this project:

  "feat: Phase D5 — song queue + auto-advance + loop / stop-next /
     spot-resume"

  "feat: Phase F2.1 — Clock Editor UI skeleton (Figma 165:2)"

  "fix: Spot Programming dialog freeze (nested QScrollArea removal +
     event.rect() clipping)"

═══════════════════════════════════════════════════════════════════════
WORKING PROTOCOL
═══════════════════════════════════════════════════════════════════════

  1. Phase the build with screenshot checkpoints — small day → screenshot
     → my approval → next day. Don't burn through scope without sync
     points.

  2. Stop and ask before destructive actions (delete, schema changes,
     git operations beyond the standard add/commit/push flow).

  3. Push back when my technical call is wrong — silence on a bad call
     is worse than a polite challenge. Past sessions caught data-model
     issues by reality-checking schema (PRAGMA table_info()) before
     coding.

  4. Trust schema reality over my spec — if a column name in my message
     differs from what PRAGMA shows, schema wins.

  5. Honest stubs — flag known TBDs in handoff. Validate / AI Optimise
     buttons in F2 are stubs by design (Phase E).

  6. Acknowledge incidents honestly. Three incidents on record:
     • DELETE-with-LIKE wiped 15 campaigns (recovery script ran)
     • Spot Programming dialog freeze (nested QScrollArea)
     • Phase D4 Studio crash (`import os` missing + sqlite3.Row.get
       — both fixed in same D4 commit)

  7. Day-by-day cadence: pose Q1-QN questions before each day; wait
     for answers + GO; build; commit; push; propose next day's Q&A.
     This is the proven pattern from Phase A + B + D.

═══════════════════════════════════════════════════════════════════════
TEST + LIFECYCLE QUICK REFERENCE
═══════════════════════════════════════════════════════════════════════

Tests under pytest:

  pytest                  # 74 total, ~13s including soak
  pytest -m "not slow"    # 73 tests, ~12s — dev default
  pytest -m slow          # 1 soak test, 100-cycle stress

Studio + scheduler lifecycle:
  • main.py owns bass_init / bass_free
  • MainWindow.__init__ creates AudioEngine + SchedulerEngine
  • MainWindow.closeEvent + aboutToQuit BOTH call _cleanup_engine
    (idempotent belt-and-suspenders)
  • _cleanup_engine: stop scheduler FIRST, then engine.cleanup_all()
  • Scheduler is NOT auto-started in main.py — explicit start() needed

═══════════════════════════════════════════════════════════════════════
WHAT I'LL SAY NEXT
═══════════════════════════════════════════════════════════════════════

After you confirm you've read both context files, I'll either:

  • Answer your re-posed Q1-Q8 + give you "GO F2.1" → you build the
    Clock Editor skeleton, screenshot checkpoint, commit, push.

  • "show me what's pending" → you summarize the 8 questions concisely
    and any open follow-ups.

  • "delete core/scheduler.py first" → you do the prep cleanup commit
    (one-line removal + commit message), push, then we resume Q&A.

  • Direct change request — possibly an iteration based on something
    I noticed since the last session.

DON'T start coding new features without my explicit GO. The cadence is:
small phase → screenshot → my approval → next.

═══════════════════════════════════════════════════════════════════════
