# RadioAI Studio Pro — Session Handover (2026-05-07)

> Read this end-to-end before touching any file. The previous session hit context limit
> after 13 commits. This document captures everything the next agent needs to be
> productive within 5 minutes.

---

## TL;DR (read this first)

- **Branch:** `native-pyqt6` (active dev, push here)
- **HEAD:** `da02a2a` "Up Coming queue strictly upcoming"
- **Tests:** **312 passing** baseline (`py -m pytest tests/ -q`)
- **App:** boots clean, Studio v3 fully wired (Phase A/B/C done), History + queue filtering working, scheduler hour-detection live
- **Operator:** Kavish Sharma, KISS FM 91.5 Jaipur. Replacing Jazler SOHO. 1366×768 desktop hardware.
- **Major pending:** Phase 2 responsive layout for non-Studio screens (pilot canceled — branch `responsive-pilot-studio` preserved as failure record)
- **Known UX issue:** AUTO pill at x=1464 in 1920-wide header may be cut off on Kavish's 1366 monitor

---

## Pre-flight protocol (mandatory — paste raw output)

```
git branch --show-current     # native-pyqt6
git status                    # only .claude/scheduled_tasks.lock + design_refs PNGs
git log --oneline -3          # da02a2a top
py -m pytest tests/ -q        # 312 passed
```

If anything mismatches → STOP and surface in plain Hindi. Don't paper over.

---

## Project basics

- **Name:** RadioAI Studio Pro v2.0
- **Stack:** PyQt6 (pure native widgets — NO HTML/WebView/Flask/JS), BASS audio via `pybass3`, SQLite WAL
- **Project root:** `E:\RadioAI_v2` (Git Bash sees as `/e/RadioAI_v2`)
- **Modeled on:** Jazler SOHO (broadcast operator's mental model)
- **Run:** `py main.py` (or `py main.py --debug` for verbose logging)
- **Python:** 3.14, Qt 6.11
- **Note:** `pytest` is NOT on PATH — always use `py -m pytest`

---

## GitHub repository

- **URL:** https://github.com/kundansharma903-code/radioai-studio-pro
- **Branches:**
  - `native-pyqt6` — active development, push here
  - `main` — base / PR target (rarely touched directly)
  - `responsive-pilot-studio` — preserved failure record from Phase 2 pilot. **DO NOT DELETE.** Used for forensic review when next responsive attempt is planned.
- **Push convention:** every feature/fix lands as a single focused commit on `native-pyqt6`. Multi-piece work uses sequential commits, one push at end.

---

## Database

- **Path:** `%LOCALAPPDATA%\RadioAI\radioai.db` → `C:\Users\hp\AppData\Local\RadioAI\radioai.db`
- **Schema:** `database/schema.sql` (idempotent `CREATE TABLE IF NOT EXISTS`)
- **Seeds:** `database/seeds.sql` (`INSERT OR IGNORE` only)
- **Manager class:** `core/database.py` — thread-safe singleton, all SQL lives here
- **Key tables:**
  - `songs` (396 rows in dev DB) — track library
  - `jingle_pads` — instant jingles for the 9-tile grid in Studio
  - `campaigns` + `spot_files` + `campaign_schedule` — ad spots
  - `clocks` + `clock_slots` — Jazler-style hour rotation patterns
  - `auto_schedule` — 24×7 grid binding (day, hour) → clock_id
  - `force_clocks` — one-off overrides
  - `broadcast_log` — air log (every play landed here, History panel reads from it)
- **Inspect quick:**
  ```bash
  py -c "from core.database import Database; db=Database(); print(list(db._conn().execute('SELECT COUNT(*) FROM broadcast_log').fetchone()))"
  ```

---

## Figma reference

- **File key:** `7oN9K61g94wKx3nu44KKDF`
- **File name:** "RadioAI — Control Panel PREMIUM"
- **Page for current Studio:** "Studio Screens v3"
- **Studio v3 main frame:** `312:2` (1920×1080 — Premium Jazler Style)
- **Other frame mappings:** see `CLAUDE.md` table (Control Panel `5:2`, Songs Library `12:2`, etc.)
- **Figma MCP tools available:** `get_design_context`, `get_screenshot`, `get_metadata`, `get_variable_defs` — read-only is fine, avoid write operations.

**Note:** old `182:2` "Studio Single Deck" mapping is **superseded** — was replaced by Studio v3. CLAUDE.md was updated in commit `35fe0d4` to reflect this. Don't fetch `182:2`.

---

## Architecture map

| Layer | File | Notes |
|---|---|---|
| Constants | `core/constants.py` | `WINDOW_W=1920, WINDOW_H=1080`. All colors, magic numbers. |
| Database | `core/database.py` | Thread-safe singleton, ~2300 lines, all SQL here |
| Settings | `core/settings.py` | Singleton cache of settings table |
| Audio Engine | `core/audio/engine.py` | **Phase A LOCKED** — multi-channel BASS + Qt signals. DO NOT MODIFY without explicit Phase A approval. |
| BASS lifecycle | `core/audio_engine.py` | Just `bass_init()` / `bass_free()` — class definition is legacy/orphaned but still imported, harmless |
| Scheduler | `core/scheduler/engine.py` | 1Hz tick on QThread, signal-driven dispatch. Has `peek_next(n)` for non-destructive preview (added `42a6160`) |
| Instant Jingles | `core/instant_jingle_engine.py` | Adapter over AudioEngine, 8-pad polyphony cap |
| Style | `assets/style.qss`, `assets/premium.qss` | QSS — applied at app boot |
| Main window | `ui/main_window.py` | 1920×1080 shell + QStackedWidget for screens |
| Entry point | `main.py` | Logging → BASS → DB → Settings → QApplication → MainWindow |
| **Studio screen** | `ui/studio.py` | **3700+ lines** — current working file. Studio v3 + all wiring. |
| Studio legacy | `ui/studio_legacy.py` | Pre-v3 backup. **DO NOT TOUCH** until on-air verified. Rollback insurance. |
| Hub / Playlists / etc. | `ui/scheduling_hub.py`, `ui/playlists.py`, etc. | Each is 1440×900 native size, top-left anchored in 1920×1080 main stack |

### Studio v3 paint widgets (inside `ui/studio.py`)

- `_Header` — 1920×72 chrome bar (logo, clock, Active Station card, status pills, Control Panel button, Settings cog). Pills at x=1320, 1392, 1464 (SIGNAL / STREAM / AUTO).
- `_NowPlayer` — 836×88 master strip (vinyl, ON-AIR LIVE pulse, waveform with playhead glow, REMAINING box)
- `_NextChip` — 200×88 NEXT-pill chip
- `_ControlCluster` — 296×80 Restart / Loop / Pause / StopNext buttons
- `_LevelMeters`, `_AnalogClock`, `_Wordmark` — master strip extras
- `_UpComingCard` × 5 inside `_UpComingQueue` — 380×820 left panel
- `_LibrariesPanel` — 720×820 center panel (7 type tiles + 5-button Action Stack + Songs table + filter)
- `_InstantJinglesPanel` — 420×540 (3×3 jingle tiles + DEMO display + 1-5 hotkeys + Edit Bank link)
- `_HistoryPanel` — 320×540 (12 alternating rose/amber rows + View Full History link)
- `_NextBreakPanel`, `_RDSPanel`, `_ProblemsPanel` — bottom-right trio
- `_BottomTransport` — 1920×80 (▶/■, slider, AutoPlay, 6-button cluster)

---

## Recent session timeline (this session — 2026-05-06 / 07)

Sequential commits on `native-pyqt6`:

| Commit | Scope |
|---|---|
| `35fe0d4` | docs(claude): update Studio frame mapping to Figma 312:2 |
| `a3229b3` | docs(night-log): backfill Studio v3 9-step rebuild entries |
| `f07b658` | feat(studio): **Phase A** — wire Instant Jingles to core.instant_jingle_engine. 9 tiles bound to DB pads, 1-5 hotkeys, Esc-stop-all, DEMO countdown. **Option 2 IJE topology** (independent instances per screen — flagged for future consolidation). |
| `42a6160` | feat(scheduler): add non-destructive `peek_next(n)` — Save-call-restore pattern, snapshots `_clock_slot_cursor` + `_active_hour_key` + `_fired_breaks` |
| `c76d753` | feat(studio): **Phase B** — Up Coming wires to `scheduler.peek_next(5)` with live AT timestamps via 1Hz tick. `_UpComingQueue.set_queue` gained `next_index` param. |
| `e1dadb4` | feat(studio): **Phase C** — Play / Stop / AUTO pill / SIGNAL polling. New `_auto_advance_enabled` flag for Live-Assist mode. Stop button narrowed to deck-only (broadcast-critical bug fix). |
| `bd1f9b8` | fix(studio): **Visual polish 1** — bg radial glows, panel drop shadows, vinyl halo, LIVE pulse animation |
| `13eb640` | fix(studio): **Visual polish 2** — waveform playhead glow + top dot, NowPlayer/NextChip 3px accent stripes, AUTO pulse, selected library row left bar, Inter font verify |
| `07347f1` | chore: fix false-positive "Inter Semi Bold" warning |
| `999ea96` | feat(studio): **active clock indicator** in header + scheduler `active_clock_changed` signal. Active Station card 3rd line shows "● <clock name>" when assigned. |
| `1e9eb49` | fix(studio): **defer spot** — wait for song EOS instead of mid-song cut. New `_pending_spot_campaign_id` state. Stop-next/Loop/AUTO-off all clear pending. |
| `d8e47a0` | fix(studio): refresh NowPlayer/NextChip/RDS when spot starts (single missing `_apply_playing_state(self._current_track)` call) |
| `7e3ef5b` | fix(studio): **always log every play** + refresh History on every play-start. Removed `if clock_id is not None:` gate. `was_manual=1` for non-scheduler plays. |
| `c828009` | fix(studio): **dedupe rapid-fire log_play** + collapse consecutive dupes in History rendering |
| `8c3b803` | feat(studio): refresh active clock + Up Coming on **showEvent + AUTO toggle + Studio's own 1Hz tick**. 4 refresh paths converge through `_set_displayed_active_clock`. |
| `da02a2a` | fix(studio): **Up Coming strictly upcoming** — exclude played + currently-playing from the panel. `_played_song_ids` set. (← HEAD) |

### Key state attributes added this session (in `ui/studio.py`)

- `_pending_spot_campaign_id: Optional[int]` — deferred spot
- `_displayed_active_clock_id: int` (-2 sentinel = never set) — header dedupe
- `_played_song_ids: set` — Up Coming filter
- `_last_song_log_id` + `_last_song_log_time` — rapid-fire log_play guard
- `_auto_advance_enabled: bool` — Live-Assist toggle
- `_upcoming_preview: list[dict]` — scheduler-driven Up Coming cache

### Failed / canceled attempts (preserve as record)

- **Phase 2 Part 1 — responsive container pilot** on branch `responsive-pilot-studio`. Wrapped Studio in `QGraphicsView+QGraphicsScene` to auto-fit on 1366×768. Operator tested and reported breakage; rolled back via `git checkout native-pyqt6`. **Branch preserved** — do not delete. Forensic review needed before next responsive attempt. Likely failure modes: QShortcut focus chain through proxy, modal QMessageBox parenting, performance with stacked QGraphicsEffect.

---

## Established conventions (discipline rules)

These were either inherited or established this session. **Follow these without being told.**

### Communication style

- **Hindi-English mix** is fine and preferred for operator-facing summaries
- **Plain Hindi** required for verification questions to operator (broadcast-language, not coder-language)
- **Senior-dev pushback ON** — surface contradictions between prompt and source code before editing
- **Investigation-first** — read 3-4 files, post 4-line summary, wait for confirmation BEFORE editing
- **Trust-but-verify** — source code wins over docs/prompts
- **Brief responses** — narrate intent in 1-2 sentences before tool calls; long planning prose annoys operator

### Coding style

- **Cache QGradient/QColor/QFont in `__init__`** (performance invariant)
- **Partial repaints via `self.update(QRect)`** not bare `self.update()`
- **NO db calls in paintEvent**, NO `self.update()` inside paintEvent
- **NO `setMouseTracking`** unless cursor change needed
- **Drop shadows via `QGraphicsDropShadowEffect`** (one effect per widget — Qt limitation)
- **Engine signals throttled** — position ≤4Hz, level meters ≤30Hz
- **Try/except every scheduler `_on_tick` path** — broadcast safety, never freeze 1Hz heartbeat
- **`hasattr(scheduler, "signal_name")` defensive guards** when adding new scheduler signals — backward compat with `_FakeScheduler` mocks in existing tests

### Test discipline

- **Live-DB fixtures use unique prefix** (`_test_studio_<uuid8>`, `_test_active_clock_<uuid8>`) + `try/finally` cleanup
- **Mock pattern:** `_FakeEngine` / `_FakeScheduler` / `_FakeIJE` per topic in test files. `_RecordingSignal` for pyqtSignal stand-in
- **Don't modify existing test files** — add to topical files or create new ones
- **Run focused tests after each piece** before full suite
- **Full suite must stay green** — zero regressions tolerance
- **Wall-clock-boundary flake** in `test_live_at_tick_refreshes_via_existing_1hz_timer` — pre-existing 1100ms wait race. Re-run usually passes.

### Commit discipline

- **One focused commit per task** — heredoc commit message format
- **Commit message body:** problem + root cause + fix + test count delta. Include "Co-Authored-By: Claude Opus 4.7 (1M context)"
- **No `--no-verify` / `--amend`** — always create new commits, hooks must pass
- **Push to `native-pyqt6` only** unless on a deliberate feature branch
- **Don't `git add -A`** — explicitly add files

### Destructive ops protocol (CRITICAL — caused real incident on 2026-05-04)

- **Never use `LIKE` patterns** for cleanup queries (caused 15-campaign + 791-row wipe)
- **Always run `SELECT COUNT(*)` first** with the same WHERE clause, get explicit user confirmation
- **Wrap in `BEGIN TRANSACTION`** for multi-row cleanup
- **Use `db.delete_*()` methods** for tables without `ON DELETE CASCADE`

### Locked code paths (DO NOT MODIFY without explicit approval)

- `core/audio/*` — Phase A LOCKED
- `core/instant_jingle_engine.py` — read-only after Phase A wiring landed
- `ui/studio_legacy.py` — rollback insurance until on-air verified
- `ui/instant_jingles.py` (standalone screen) — has its own IJE instance per Option 2 topology
- Any other `ui/*` screen file unless explicit task targets it
- Existing test files (add new ones instead)

---

## Operator-facing features — current state

### ✅ Working

| Feature | Status |
|---|---|
| Studio v3 visual layout | Pixel-aligned to Figma 312:2, 1920×1080 |
| Audio playback (Play / Stop / Pause) | Wired through AudioEngine. Stop is deck-only (jingles survive). |
| Instant Jingles (9 tiles + 1-5 hotkeys + Esc) | DB-bound, real audio dispatch, DEMO countdown |
| AUTO pill toggle | Starts/stops scheduler + flips `_auto_advance_enabled`, with immediate refresh |
| Up Coming queue | Scheduler-driven (`peek_next`) when AUTO ON, fallback when OFF. Excludes played + currently-playing. |
| Active Clock indicator | Updates from 4 paths (1Hz tick, showEvent, AUTO toggle, scheduler signal) |
| History panel | Logs every play (manual + auto), refreshes on play-start, dedupes consecutive dupes in display |
| Spot dispatch | Defers to song-end (no mid-song cut). NowPlayer reflects spot name during airing. |
| Hour-boundary detection | Auto-loads new clock at hour rollover via scheduler tick |
| Visual polish | Bg radial glows, panel drop shadows, vinyl halo, LIVE pulse, AUTO pulse, waveform playhead glow + top dot |

### 🟡 Partially working / known limitations

- **AUTO pill at x=1464** — may be off-screen on 1366×768 monitor (Kavish's hardware). Phase 2 responsive layout would solve this; pilot canceled. Workaround: launch maximized, AUTO pill should be visible after window expands.
- **Older premium screens (Hub / Playlists / AutoSchedule / ClockEditor / PlaylistEdit)** — hardcoded `setFixedSize(1440, 900)`, top-left anchored in 1920×1080 main stack. On 1366×768 monitor → scrollbars appear. Phase 2 work pending.
- **Up Coming queue exhaustion** — when all 12 songs in `_queue_songs` played, panel shows placeholders. No auto-refill. Operator would need to navigate to Library or AUTO ON.
- **Single-slot pending spot** — if 2 spots fire while a song plays, the most-recent overwrites the earlier. Flagged for future queue-based enhancement.

### 🔴 Not yet built

- **Frame 12 (294:2) — Final Log Creator Day Picker**
- **Frame 13 (297:2) — Build Log inner**
- **Phase 2 Part 2** — propagate responsive container to non-Studio screens (after fixing pilot's QShortcut/modal/perf issues)
- **Stitcher integration** — pre-mix "Coming Up Next" block into one WAV
- **Sweeper overlay** — separate channel, timed trigger
- **AI Daily Scheduler integration** with Studio
- **RDS / Icecast streaming output** — wire SIGNAL pill to actual stream state
- **Standalone Instant Jingles screen IJE consolidation** — currently has own instance, not shared with Studio's

---

## NIGHT_LOG carry-overs (forensic record)

Read `NIGHT_LOG.md` end-to-end before starting major new work. Key past incidents:

1. **2026-05-04 — DELETE-with-LIKE wipe** — `LIKE '__%'` matched all rows ≥2 chars long → nuked 15 campaigns + 791 schedule rows. CLAUDE.md "Destructive operation protocol" was added in response.
2. **"Saved clock vanished" display bug** — Available Clocks panel sliced 3 rows + ordered by name ASC → fresh clocks with lowercase names sorted to end + got hidden. Fixed via consumer-side `id DESC` sort.
3. **Pre-existing modal-dialog hung test** — `test_preview_without_engine_does_not_crash` in `test_playlists_screen.py` hangs on PyQt 6.11 + `-platform minimal`. Permanently deselected. Not caused by recent work.
4. **Studio v3 9-step rebuild** — disciplined per-step checkpoints to avoid structural drift. Backfilled in NIGHT_LOG by `a3229b3`.
5. **Phase 2 responsive pilot canceled** — see "Failed attempts" section above.

---

## Quick start for next session

1. Run pre-flight (paste output)
2. Read this file end-to-end
3. Skim `CLAUDE.md` (project guide, Figma frames, destructive op protocol)
4. Skim `NIGHT_LOG.md` (incident history) — read at minimum the most recent 3 entries
5. Check git log to confirm HEAD matches `da02a2a` and tests pass
6. Wait for operator's task instruction — do NOT proactively pick up pending items

When the operator gives a task:
- **If it touches Studio v3** → read the relevant section of `ui/studio.py` (3700+ lines, navigate by class name) before editing
- **If it touches scheduler** → read `core/scheduler/engine.py` and any existing test for that area
- **If it's a new screen / Frame 12 or 13** → fetch Figma context first, then plan before coding
- **Always investigation-first, 4-line summary, wait for confirmation, then implement**

---

## Last words

Operator (Kavish) is patient with disciplined work but pushes back on scope creep, over-engineering, and silent papering-over. Surface true blockers in plain Hindi. Don't claim audio/visual outcomes you haven't verified — the test suite proves wire shape, not actual broadcast audio. On-air smoke is always operator-verified, not agent-claimed.

Good luck.

— Previous session, signing off at HEAD `da02a2a`, 312 tests green, app boots clean.
