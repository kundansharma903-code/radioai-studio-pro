"""RadioAI database package — schema, seeds, migrations, manager.

Import `DBManager` directly from `database.db_manager` to avoid the
`runpy` warning when running `python -m database.db_manager`.
"""
